// This file exports a simple utility for markdown processing
// The actual rendering is handled by react-markdown in the component
export function processMarkdown(content: string): string {
  // Basic preprocessing if needed
  // For now, just return the content as-is
  // react-markdown will handle the actual parsing
  return content;
}
